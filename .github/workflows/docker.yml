name: Docker Build & Push

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  # Directly define the values here (no self-referencing)
  REPO_OWNER: "kanika"  # Your lowercase GitHub username
  REPO_NAME: "ctf-platform"  # Your lowercase repo name
  FRONTEND_IMAGE: "ctf-platform-frontend"  # Hardcoded derived value
  BACKEND_IMAGE: "ctf-platform-backend"  # Hardcoded derived value

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Node Modules
        uses: actions/cache@v3
        with:
          path: |
            **/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Build Frontend
        uses: docker/build-push-action@v3
        with:
          context: ./frontend
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ env.REGISTRY }}/${{ env.REPO_OWNER }}/${{ env.FRONTEND_IMAGE }}:latest
          labels: "app=frontend"

      - name: Build Backend
        uses: docker/build-push-action@v3
        with:
          context: ./backend
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ env.REGISTRY }}/${{ env.REPO_OWNER }}/${{ env.BACKEND_IMAGE }}:latest
          labels: "app=backend"