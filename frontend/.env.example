# Backend API base URL.
# Leave empty (or set to /) when nginx/a reverse-proxy forwards /api to the backend.
# Set to the full backend URL only for cross-origin deployments (e.g. Vercel → Railway).
VITE_API_BASE_URL=

# Google OAuth Client ID (must match the one in backend .env)
VITE_GOOGLE_CLIENT_ID=replace_with_google_client_id.apps.googleusercontent.com
